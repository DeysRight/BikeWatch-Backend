spring:
  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: none
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.type: trace
